# План: Windows вариант на my_memo — Zero-change в lib/

## Принцип

**Нулева промяна** в съществуващите lib/ файлове. Всички Windows-specific промени са в нови файлове + minimally-invasive conditional imports.

---

## Архитектура

```
lib/
├── platform/                          # НОВА папка
│   ├── platform_init.dart             # Entry point — conditionally exports
│   ├── platform_init_android.dart     # Android: noop
│   ├── platform_init_windows.dart     # Windows: sqflite FFI
│   │
│   ├── image_service.dart             # Conditionally exports
│   ├── image_service_android.dart     # Android: wraps image_picker
│   ├── image_service_windows.dart     # Windows: wraps file_picker
│   │
│   ├── backup_service.dart            # Conditionally exports
│   ├── backup_service_android.dart    # Android: /storage/emulated/0/... + Google Drive
│   ├── backup_service_windows.dart    # Windows: Documents/... + no Google Drive
│   │
│   └── sharing_service.dart           # Conditionally exports
│   ├── sharing_service_android.dart   # Android: wraps receive_sharing_intent
│   └── sharing_service_windows.dart   # Windows: noop (no share target)
│
├── main.dart                          # БЕЗ ПРОМЯНА
├── note_form.dart                     # БЕЗ ПРОМЯНА
├── google_drive_helper.dart           # БЕЗ ПРОМЯНА
├── settings_screen.dart               # БЕЗ ПРОМЯНА
└── ...
```

### Как работи conditional export

```dart
// lib/platform/image_service.dart
export 'image_service_android.dart'
    if (dart.library.js_interop) 'image_service_windows.dart';
// Няма dart.library.windows в Flutter — използваме kIsWeb + Platform комбинация
```

**Проблем**: Flutter няма `dart.library.windows` conditional. Затова използваме **runtime factory** подход:

```dart
// lib/platform/image_service.dart
import 'dart:io';
import 'image_service_android.dart' as android;
import 'image_service_windows.dart' as windows;

ImageService createImageService() {
  if (Platform.isWindows) return windows.ImageServiceImpl();
  return android.ImageServiceImpl();
}
```

---

## Фаза 1: Platform init (sqflite)

### Нов файл: `lib/platform/platform_init.dart`
```dart
import 'dart:io';
import 'platform_init_android.dart' as android;
import 'platform_init_windows.dart' as windows;

void initPlatform() {
  if (Platform.isWindows) {
    windows.init();
  } else {
    android.init();
  }
}
```

### Нов файл: `lib/platform/platform_init_android.dart`
```dart
void init() {
  // Нищо — Android sqflite работи по подразбиране
}
```

### Нов файл: `lib/platform/platform_init_windows.dart`
```dart
import 'package:sqflite_common_ffi/sqflite_ffi.dart';

void init() {
  sqfliteFfiInit();
  databaseFactory = databaseFactoryFfi;
}
```

### Промяна в `lib/main.dart` (МИНИМАЛНА — 2 реда)
```dart
import 'platform/platform_init.dart';  // НОВО

void main() {
  initPlatform();  // НОВО
  final binding = WidgetsFlutterBinding.ensureInitialized();
  FlutterNativeSplash.preserve(widgetsBinding: binding);
  runApp(const BusinessOrganizerApp());
}
```

---

## Фаза 2: Image service (file_picker vs image_picker)

### Нов файл: `lib/platform/image_service.dart`
```dart
import 'dart:io';
import 'platform_init_android.dart' as android;
import 'platform_init_windows.dart' as windows;

abstract class ImageService {
  Future<String?> pickImage();
}

ImageService createImageService() {
  if (Platform.isWindows) return windows.ImageServiceImpl();
  return android.ImageServiceImpl();
}
```

### Нов файл: `lib/platform/image_service_windows.dart`
```dart
import 'package:file_picker/file_picker.dart';

class ImageServiceImpl implements ImageService {
  @override
  Future<String?> pickImage() async {
    final result = await FilePicker.platform.pickFiles(type: FileType.image);
    return result?.files.single.path;
  }
}
```

### Нов файл: `lib/platform/image_service_android.dart`
```dart
import 'package:image_picker/image_picker.dart';

class ImageServiceImpl implements ImageService {
  @override
  Future<String?> pickImage() async {
    final picker = ImagePicker();
    final picked = await picker.pickImage(source: ImageSource.gallery);
    return picked?.path;
  }
}
```

### Промяна в `lib/note_form.dart` (МИНИМАЛНА — 1 ред)
```dart
import 'platform/image_service.dart';  // НОВО (замества import на image_picker)
// Всички останали image_picker импорти остават, но вече не се ползват директно
```

**Забележка**: note_form.dart вече има complex логика около image_picker (crop, и т.н.). За Windows ще:
- Използваме file_picker за избор
- Crop ще бъде деактивиран (image_cropper не работи на Windows)
- Това става чрез platform-specific файловете, НЕ чрез промяна на note_form.dart

---

## Фаза 3: Backup service (локален path + Google Drive)

### Нов файл: `lib/platform/backup_service.dart`
```dart
import 'dart:io';

abstract class BackupService {
  Future<String> getBackupDirectory();
  bool get supportsGoogleDrive;
}

BackupService createBackupService() {
  if (Platform.isWindows) return BackupServiceWindows();
  return BackupServiceAndroid();
}
```

### Нов файл: `lib/platform/backup_service_windows.dart`
```dart
import 'package:path_provider/path_provider.dart';
import 'package:path/path.dart' as p;

class BackupServiceWindows implements BackupService {
  @override
  bool get supportsGoogleDrive => false;

  @override
  Future<String> getBackupDirectory() async {
    final dir = await getApplicationDocumentsDirectory();
    return p.join(dir.path, 'my_memo_backups');
  }
}
```

### Нов файл: `lib/platform/backup_service_android.dart`
```dart
class BackupServiceAndroid implements BackupService {
  @override
  bool get supportsGoogleDrive => true;

  @override
  Future<String> getBackupDirectory() async {
    return '/storage/emulated/0/Download/my_memo_backups';
  }
}
```

### Промяна в `lib/google_drive_helper.dart` (МИНИМАЛНА — 3 реда)
```dart
import 'platform/backup_service.dart';  // НОВО

// В saveBackupLocally() и listLocalBackups():
final backupService = createBackupService();  // НОВО
final backupDirPath = await backupService.getBackupDirectory();  // НОВО
// Замества hardcoded '/storage/emulated/0/Download/...'
```

### Промяна в `lib/settings_screen.dart` (МИНИМАЛНА — 2 реда)
```dart
import 'platform/backup_service.dart';  // НОВО

// В build(), около Google Drive секцията:
final backupService = createBackupService();
if (backupService.supportsGoogleDrive) ...[
  // Съществуващ Google Drive UI
]
```

---

## Фаза 4: Sharing service (receive_sharing_intent)

### Нов файл: `lib/platform/sharing_service.dart`
```dart
import 'dart:io';
import 'sharing_service_android.dart' as android;
import 'sharing_service_windows.dart' as windows;

abstract class SharingService {
  void init(void Function(List<String> paths) onShared);
  void dispose();
  Future<List<String>> getInitialMedia();
}

SharingService createSharingService() {
  if (Platform.isWindows) return windows.SharingServiceImpl();
  return android.SharingServiceImpl();
}
```

### Нов файл: `lib/platform/sharing_service_windows.dart`
```dart
class SharingServiceImpl implements SharingService {
  @override
  void init(void Function(List<String> paths) onShared) {
    // Windows няма share target — noop
  }
  @override
  void dispose() {}
  @override
  Future<List<String>> getInitialMedia() async => [];
}
```

### Нов файл: `lib/platform/sharing_service_android.dart`
```dart
// Wraps receive_sharing_intent
```

### Промяна в `lib/main.dart` (МИНИМАЛНА — 2 реда)
```dart
import 'platform/sharing_service.dart';  // НОВО

// В _initializeApp():
final sharingService = createSharingService();  // НОВО
sharingService.init((paths) { ... });  // НОВО
// Замества ReceiveSharingIntent.instance.getMediaStream()
```

---

## Фаза 5: pubspec.yaml

```yaml
dependencies:
  # ... съществуващи ...
  sqflite_common_ffi: ^2.3.0  # НОВО — Windows SQLite
  file_picker: ^6.1.0          # НОВО — Windows файлов избор
```

---

## Фаза 6: Windows build config

**`windows/CMakeLists.txt`**:
```cmake
set(BINARY_NAME "my_memo")     # от my_scr
project(my_memo LANGUAGES CXX)  # от my_scr
```

**`windows/runner/Runner.rc`**:
- FileDescription: "my memo"
- OriginalFilename: "my_memo.exe"

**`windows/runner/resources/app_icon.ico`**:
- Генериране от `assets/app_icon.png`

---

## Сводка на промените

| Файл | Тип промяна |
|---|---|
| `lib/platform/**` (8 нови файла) | НОВИ — Windows implementations |
| `lib/main.dart` | 3 добавени реда (import + 2 vola calls) |
| `lib/note_form.dart` | 1 добавен ред (import) |
| `lib/google_drive_helper.dart` | 3 добавени реда (import + backupService) |
| `lib/settings_screen.dart` | 2 добавени реда (import + conditional UI) |
| `pubspec.yaml` | 2 нови зависимости |
| `windows/CMakeLists.txt` | Rename |
| `windows/runner/Runner.rc` | Rename |
| `windows/runner/resources/app_icon.ico` | НОВ |

**Съществуващият Android код**: 100% непроменен. Всички conditionals са в новите platform файлове.

---

## Верификация

1. `flutter pub get`
2. `flutter analyze` — без грешки
3. `flutter build windows` — успешен build
4. `flutter run -d windows` — приложението стартира
5. Android тест: `flutter build apk` — без regressions
6. Тестове на Windows:
   - Създаване/редактиране/изтриване на бележка
   - Файлов избор чрез system dialog
   - Локален backup/restore
   - Google Drive секцията липсва
   - Receive sharing не crash-ва
